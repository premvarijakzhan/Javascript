Object.getPrototypeOf(myObject);

//myObject.__proto__ (parent of object)
//assume we have myObject created by the constructor
//Constructor has prototype property == Constructor.prototype() what we will have in the bracket is exactly 
//in the first bracket 
 
function Circle(radius) {
    this.radius = radius;
}

Circle.prototype



const Circle = new Circle(1);