
let numbers = (1, 10);

console.log(numbers);

function maxValues(number1, number2) {
    // if (number1 > number2) return number1;
    // else return number2; 
    // return number2;

    //(condition) ? true : else
    return (number1>number2) ? number1 :number2;
}
